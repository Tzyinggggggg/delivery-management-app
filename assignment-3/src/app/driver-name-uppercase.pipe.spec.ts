import { DriverNameUppercasePipe } from './driver-name-uppercase.pipe';

describe('DriverNameUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new DriverNameUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
